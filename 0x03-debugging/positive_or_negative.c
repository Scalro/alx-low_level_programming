#include "main.h"

/**
 * positive_or_negative - this is a main function
 * @i: number checked
 * Return: returns 0
 */

void positive_or_negative(int i)
{
	if (i < 0)
		printf("%d is negative\n", i);
	else
		printf("%d is positive\n", i);
}

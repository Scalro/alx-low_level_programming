#include <stdio.h>
/**
 * main - display the main function which is the entry point of a program
 * Return: it return 0
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
